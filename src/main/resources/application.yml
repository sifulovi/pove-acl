spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/povedb
    username: pove
    password: pove321
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
springdoc:
  pathsToMatch: /api/**
